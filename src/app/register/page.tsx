import { RegisterForm } from '@components/FormAuth/RegisterForm';

// O FATO DO COMPONENTE SER CLIENT NAO CONSEGUE ACESSAR AINDA O ENV, TENTE DAR UM JEITO NISSO DEPOIS

export default function Register() {
  return <RegisterForm />;
}
