import { type ReactElement } from 'react';

import AboutProjectPage from '../components/AboutProjectPage';

export default function AboutProject(): ReactElement {
  return (
    <main className="bg-input flex flex-col">
      <AboutProjectPage />
    </main>
  );
}
